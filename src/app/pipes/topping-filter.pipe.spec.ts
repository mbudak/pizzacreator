import { ToppingFilterPipe } from './topping-filter.pipe';

describe('ToppingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ToppingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
